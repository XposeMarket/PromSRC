name: Temporary approval summarizer staging

on:
  push:
    branches:
      - agent/chat-approval-model
    paths:
      - .github/workflows/tmp-stage-chat-approval-summarizer.yml

permissions:
  contents: write

jobs:
  stage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/chat-approval-model
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Stage approval summarizer
        run: node scripts/stage-chat-approval-summarizer.mjs
      - name: Syntax-check model
        run: node --check web-ui/src/features/chat/approvals/model.js
      - name: Commit staging module
        run: |
          rm scripts/stage-chat-approval-summarizer.mjs
          rm .github/workflows/tmp-stage-chat-approval-summarizer.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "refactor(chat): stage approval summarizer"
          git push origin HEAD:agent/chat-approval-model
