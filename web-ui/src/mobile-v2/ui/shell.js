import { ICONS } from './icons.js';
import { sessionRef } from '../core/gateway-manager.js';
const TABS=[{id:'chat',label:'Chat',icon:'chat'},{id:'voice',label:'Voice',icon:'mic'},{id:'tasks',label:'Tasks',icon:'clipboard'},{id:'hub',label:'Hub',icon:'person'}];
const DRAWER_ITEMS=[{id:'schedule',label:'Schedule',icon:'calendar'},{id:'teams',label:'Teams',icon:'users'},{id:'subagents',label:'Subagents',icon:'robot'},{id:'proposals',label:'Proposals',icon:'doc'},{id:'more',label:'More',icon:'dots'}];
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));}
function formatWhen(value){const numeric=Number(new Date(value||0));if(!numeric)return'';const delta=Math.max(0,Date.now()-numeric);if(delta<60000)return'now';if(delta<3600000)return`${Math.floor(delta/60000)}m`;if(delta<86400000)return`${Math.floor(delta/3600000)}h`;return`${Math.floor(delta/86400000)}d`;}
function sessionStamp(session){return Number(new Date(session.updatedAt||session.lastActivityAt||session.lastMessageAt||session.createdAt||0));}
export function createMobileV2Shell({root,gateways}){
  let navigate=()=>{};let drawerOpen=false;let activeTab='chat';let sessions=[];let disposed=false;
  root.innerHTML=`<div class="pm-app pm-v2-app" id="pm-v2-app"><section class="pm-drawer pm-v2-drawer" id="pm-v2-drawer" aria-hidden="true"><div class="pm-v2-drawer-top"><button class="pm-icon-btn pm-v2-drawer-close" type="button" aria-label="Close menu">${ICONS.menu}</button><div class="pm-v2-drawer-brand">Prometheus</div><button class="pm-icon-btn" type="button" data-v2-connections aria-label="Gateway Connections">${ICONS.users}</button></div><div class="pm-v2-gateway-strip" data-v2-gateway-strip></div><button class="pm-drawer-new-chat pm-v2-new-chat" type="button">${ICONS.plus}<span>New chat</span></button><div class="pm-v2-drawer-label">Chats</div><div class="pm-drawer-list pm-v2-session-list" id="pm-v2-session-list"></div><div class="pm-v2-drawer-label pm-v2-nav-label">Prometheus</div><nav class="pm-drawer-list pm-v2-drawer-nav" aria-label="Prometheus sections">${DRAWER_ITEMS.map(item=>`<button class="pm-drawer-item pm-v2-drawer-item" type="button" data-drawer-route="${item.id}"><span class="pm-flex"><span class="pm-v2-drawer-icon">${ICONS[item.icon]}</span><span>${item.label}</span></span><span class="pm-v2-session-chevron">${ICONS.chevron}</span></button>`).join('')}</nav></section><div class="pm-page pm-v2-page" id="pm-v2-page"><header class="pm-header pm-v2-header"><button class="pm-icon-btn pm-v2-menu" type="button" aria-label="Open menu">${ICONS.menu}</button><div class="pm-v2-header-center"><div class="pm-v2-brand">Prometheus</div></div><button class="pm-online pm-v2-status" id="pm-v2-status" type="button"><span class="pm-v2-status-dot"></span><span>Checking</span></button></header><main class="pm-body pm-v2-body" id="pm-v2-page-slot"></main></div><nav class="pm-tabbar pm-v2-tabbar" id="pm-v2-tabbar" aria-label="Primary navigation"><span class="pm-tab-indicator" aria-hidden="true"></span>${TABS.map(tab=>`<button class="pm-tab${tab.id===activeTab?' active':''}" type="button" data-tab="${tab.id}" aria-label="${tab.label}"><span class="pm-tab-icon">${ICONS[tab.icon]}</span><span class="pm-tab-label">${tab.label}</span></button>`).join('')}</nav><div class="pm-v2-notice" id="pm-v2-notice" hidden></div></div>`;
  const page=root.querySelector('#pm-v2-page-slot');const drawer=root.querySelector('#pm-v2-drawer');const list=root.querySelector('#pm-v2-session-list');const tabbar=root.querySelector('#pm-v2-tabbar');const status=root.querySelector('#pm-v2-status');const notice=root.querySelector('#pm-v2-notice');const gatewayStrip=root.querySelector('[data-v2-gateway-strip]');
  function setDrawer(open){drawerOpen=!!open;drawer.classList.toggle('open',drawerOpen);drawer.setAttribute('aria-hidden',drawerOpen?'false':'true');document.body.classList.toggle('pm-v2-drawer-open',drawerOpen);if(drawerOpen){refreshSessions();refreshGatewayStrip();}}
  function positionIndicator(){const target=tabbar?.querySelector(`.pm-tab[data-tab="${activeTab}"]`);const indicator=tabbar?.querySelector('.pm-tab-indicator');if(!target||!indicator)return;indicator.style.setProperty('--pm-ind-x',`${target.offsetLeft}px`);indicator.style.setProperty('--pm-ind-w',`${target.offsetWidth}px`);tabbar.style.setProperty('--pm-lens-x',`${target.offsetLeft+target.offsetWidth/2}px`);}
  function setActiveTab(tabId){activeTab=TABS.some(tab=>tab.id===tabId)?tabId:'chat';tabbar?.querySelectorAll('.pm-tab').forEach(button=>button.classList.toggle('active',button.dataset.tab===activeTab));requestAnimationFrame(positionIndicator);}
  function refreshGatewayStrip(){const rows=gateways.list();gatewayStrip.innerHTML=rows.map(entry=>`<button type="button" class="pm-v2-gateway-pill${entry.id===gateways.activeId?' active':''}" data-gateway-id="${escapeHtml(entry.id)}"><span class="pm-v2-gateway-dot ${escapeHtml(entry.status||'unknown')}"></span>${escapeHtml(entry.name)}</button>`).join('');}
  function renderSessions(){list.innerHTML=sessions.length?sessions.map(session=>`<button class="pm-session-row pm-v2-session-row" type="button" data-session-ref="${escapeHtml(session._ref)}"><span class="pm-v2-session-copy"><strong>${escapeHtml(session.title||session.name||'Untitled chat')}</strong><small><span class="pm-session-gateway">${escapeHtml(session._gatewayName)}</span>${formatWhen(session.updatedAt||session.lastActivityAt||session.createdAt)?` · ${formatWhen(session.updatedAt||session.lastActivityAt||session.createdAt)}`:''}</small></span><span class="pm-v2-session-chevron">${ICONS.chevron}</span></button>`).join(''):'<div class="pm-v2-drawer-empty">No chats yet.</div>';}
  async function refreshSessions(){list.innerHTML='<div class="pm-v2-drawer-empty">Loading chats…</div>';const results=await Promise.allSettled(gateways.list().map(async entry=>{const result=await gateways.client(entry.id).listSessions({state:'all',limit:80});return result.sessions.map(session=>({...session,_gatewayId:entry.id,_gatewayName:entry.name,_ref:sessionRef(entry.id,session.id)}));}));if(disposed)return;sessions=results.flatMap(result=>result.status==='fulfilled'?result.value:[]).sort((a,b)=>sessionStamp(b)-sessionStamp(a));renderSessions();}
  async function newChat(){const gateway=gateways.active;const id=`mobile_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;try{await gateway.createSession({id,title:'New Chat'});const ref=gateways.bindSession(id,gateway.id)||sessionRef(gateway.id,id);setDrawer(false);navigate(`chat/${encodeURIComponent(ref)}`);refreshSessions();}catch(error){showNotice(error?.message||'Could not create a new chat.');}}
  async function refreshStatus(){const entry=await gateways.probe(gateways.activeId);if(disposed||!entry)return;status.classList.toggle('offline',entry.status!=='online');status.lastElementChild.textContent=entry.status==='online'?entry.name:(entry.status==='revoked'?'Reconnect':'Offline');refreshGatewayStrip();}
  function showNotice(message){notice.textContent=String(message||'');notice.hidden=!message;if(message)window.setTimeout(()=>{if(notice.textContent===message)notice.hidden=true;},6000);}
  root.querySelector('.pm-v2-menu')?.addEventListener('click',()=>setDrawer(true));root.querySelector('.pm-v2-drawer-close')?.addEventListener('click',()=>setDrawer(false));root.querySelector('.pm-v2-new-chat')?.addEventListener('click',newChat);root.querySelector('[data-v2-connections]')?.addEventListener('click',()=>{setDrawer(false);navigate('gateways');});status?.addEventListener('click',()=>navigate('gateways'));
  list?.addEventListener('click',event=>{const row=event.target.closest('[data-session-ref]');if(!row)return;setDrawer(false);navigate(`chat/${encodeURIComponent(row.dataset.sessionRef)}`);});drawer?.addEventListener('click',event=>{const routeButton=event.target.closest('[data-drawer-route]');if(routeButton){setDrawer(false);navigate(routeButton.dataset.drawerRoute);return;}const gatewayButton=event.target.closest('[data-gateway-id]');if(gatewayButton){gateways.select(gatewayButton.dataset.gatewayId);refreshGatewayStrip();refreshStatus();refreshSessions();}});tabbar?.addEventListener('click',event=>{const button=event.target.closest('[data-tab]');if(button)navigate(button.dataset.tab);});window.addEventListener('resize',positionIndicator,{passive:true});gateways.addEventListener('change',refreshStatus);
  requestAnimationFrame(positionIndicator);refreshStatus();
  return{page,gateways,navigate(route){navigate(route);},setNavigate(callback){navigate=callback||(()=>{});},setActiveTab,setDrawer,refreshSessions,refreshStatus,showNotice,setTitle(title){const brand=root.querySelector('.pm-v2-brand');if(brand)brand.textContent=title||'Prometheus';},dispose(){disposed=true;gateways.removeEventListener('change',refreshStatus);}};
}
