games/figure-8-drift/index.html (161 lines) [window 91-156, truncated]:
91: function applyTrackSnapshot(snap){if(!snap||!Array.isArray(snap.segments))return false;if(snap.isPreset&&snap.segments.length===1&&snap.segments[0].type==='preset'){segments=[{...PRESET_F8}];trackClosed=true}else{segments=snap.segments.map(x=>({...x}));trackClosed=!!snap.trackClosed}rebuildTrack();reset();updateSegUI();return true}
92: function saveTrackToSlot(){if(isPreset()){ui.msg.innerHTML='SAVE TRACK<span class="sub">Add straights/turns first — figure-8 can’t be saved as custom.</span>';return}const name=prompt('Track name','My track');if(name==null)return;const list=readTrackLibrary();const entry={name:(name.trim()||'My track').slice(0,40),...trackSnapshot()};const ix=list.findIndex(t=>t.name===entry.name);if(ix>=0)list[ix]=entry;else list.unshift(entry);writeTrackLibrary(list);refreshTrackLoadSelect();refreshMenuTrackSelect();ui.msg.innerHTML='SAVED<span class="sub">“'+entry.name+'” stored on this device.</span>'}
93: function loadTrackFromSlot(){const sel=document.getElementById('trackLoadSel');if(!sel||!sel.value)return;const list=readTrackLibrary();const t=list.find(x=>String(x.savedAt)===sel.value);if(!t)return;applyTrackSnapshot(t);ui.msg.innerHTML='LOADED<span class="sub">'+t.name+'</span>'}
94: function deleteTrackSlot(){const sel=document.getElementById('trackLoadSel');if(!sel||!sel.value)return;if(!confirm('Delete this saved track?'))return;writeTrackLibrary(readTrackLibrary().filter(x=>String(x.savedAt)!==sel.value));refreshTrackLoadSelect();refreshMenuTrackSelect()}
95: function refreshTrackLoadSelect(){const sel=document.getElementById('trackLoadSel');if(!sel)return;const list=readTrackLibrary(),v=sel.value;sel.innerHTML='<option value="">— saved —</option>'+list.map(t=>'<option value="'+t.savedAt+'">'+t.name.replace(/</g,'')+'</option>').join('');if([...sel.options].some(o=>o.value===v))sel.value=v}
96: function refreshMenuTrackSelect(){const sel=document.getElementById('menuTrackSel');if(!sel)return;const list=readTrackLibrary(),v=sel.value;sel.innerHTML='<option value="">— saved tracks —</option>'+list.map(t=>'<option value="'+t.savedAt+'">'+t.name.replace(/</g,'')+'</option>').join('');if([...sel.options].some(o=>o.value===v))sel.value=v}
97: function syncTrackSelects(from){const a=document.getElementById('trackLoadSel'),b=document.getElementById('menuTrackSel');if(from==='menu'&&a&&b)a.value=b.value;else if(a&&b)b.value=a.value}
98: function switchBuildTab(name){for(const n of['Road','Track','Mode']){document.getElementById('tab'+n)?.classList.toggle('on',n===name);document.getElementById('group'+n)?.classList.toggle('on',n===name)}}
99: function setBuildMode(on){buildMode=on;document.body.classList.toggle('build-mode',on);document.body.classList.toggle('drive-mode',!on);document.getElementById('modeBuild')?.classList.toggle('on',on);document.getElementById('modeDrive')?.classList.toggle('on',!on);car.visible=!on;if(on&&center.length){const p=center[Math.floor(center.length/2)];buildCam.px=p.x;buildCam.pz=p.z;buildCam.smPx=p.x;buildCam.smPz=p.z;buildCam.smYaw=buildCam.yaw;buildCam.smPitch=buildCam.pitch;buildCam.smDist=buildCam.dist}}
100: function ensureCustom(){if(isPreset()){segments=[];trackClosed=false}}
101: function addStraightPiece(){ensureCustom();segments.push({type:'straight',len:+document.getElementById('straightLen').value});rebuildTrack();reset()}
102: function addTurnPiece(turn){ensureCustom();segments.push({type:turn,deg:+document.getElementById('turnDeg').value,r:+document.getElementById('turnRad').value});rebuildTrack();reset()}
103: function loadPresetF8(){segments=[{...PRESET_F8}];rebuildTrack();reset();ui.msg.innerHTML='FIGURE 8 DRIFT<span class="sub">Endless laps — drift for score.</span>'}
104: function openMenu(){ui.start.style.display='flex';started=false;paused=false;finished=false;touch.steer=touch.gas=touch.brake=touch.hand=0;velX=velZ=0}
105: document.getElementById('modeBuild')?.addEventListener('click',()=>setBuildMode(true));
106: document.getElementById('modeDrive')?.addEventListener('click',()=>{setBuildMode(false);reset()});
107: document.getElementById('presetF8')?.addEventListener('click',loadPresetF8);
108: document.getElementById('menuBtn')?.addEventListener('click',openMenu);
109: document.getElementById('menuBtnDrive')?.addEventListener('click',openMenu);
110: document.getElementById('addStraight')?.addEventListener('click',addStraightPiece);
111: document.getElementById('addTurnL')?.addEventListener('click',()=>addTurnPiece('turnL'));
112: document.getElementById('addTurnR')?.addEventListener('click',()=>addTurnPiece('turnR'));
113: document.getElementById('undoSeg')?.addEventListener('click',()=>{if(isPreset())return;segments.pop();rebuildTrack();reset()});
114: document.getElementById('clearSeg')?.addEventListener('click',()=>{segments=[];trackClosed=false;rebuildTrack();reset()});
115: document.getElementById('doneTrack')?.addEventListener('click',()=>{if(!isPreset()&&!segments.length)segments.push({type:'straight',len:32});rebuildTrack();setBuildMode(false);reset()});
116: document.getElementById('saveTrackBtn')?.addEventListener('click',saveTrackToSlot);
117: document.getElementById('loadTrackBtn')?.addEventListener('click',loadTrackFromSlot);
118: document.getElementById('delTrackBtn')?.addEventListener('click',deleteTrackSlot);
119: for(const n of['Road','Track','Mode'])document.getElementById('tab'+n)?.addEventListener('click',()=>switchBuildTab(n));
120: document.getElementById('trackLoadSel')?.addEventListener('change',()=>syncTrackSelects('editor'));
121: document.getElementById('menuTrackSel')?.addEventListener('change',()=>syncTrackSelects('menu'));
122: document.getElementById('startDriveBtn')?.addEventListener('click',()=>{ui.start.style.display='none';started=true;last=performance.now();setBuildMode(false);reset();requestAnimationFrame(loop)});
123: document.getElementById('startBuildBtn')?.addEventListener('click',()=>{ui.start.style.display='none';started=true;last=performance.now();setBuildMode(true);switchBuildTab('Road');requestAnimationFrame(loop)});
124: document.getElementById('menuPresetBtn')?.addEventListener('click',()=>{loadPresetF8();refreshTrackLoadSelect();refreshMenuTrackSelect()});
125: document.getElementById('menuCamBtn')?.addEventListener('click',()=>camMode=(camMode+1)%2);
126: document.getElementById('menuLoadBtn')?.addEventListener('click',()=>{syncTrackSelects('menu');loadTrackFromSlot();ui.start.style.display='none';started=true;last=performance.now();setBuildMode(false);requestAnimationFrame(loop)});
127: document.getElementById('menuDelBtn')?.addEventListener('click',()=>{syncTrackSelects('menu');deleteTrackSlot()});
128: function nearestProgress(x,z){let best=1e9,bi=0;for(let i=0;i<N;i+=2){const p=center[i],d=(p.x-x)*(p.x-x)+(p.z-z)*(p.z-z);if(d<best){best=d;bi=i}}return{u:bi/N,dist:Math.sqrt(best),i:bi}}
129: function reset(){syncCarToStart();velX=velZ=0;laps=0;passed=0;score=0;combo=1;bestDrift=0;offroad=0;finished=false;if(isPreset())ui.msg.innerHTML='FIGURE 8 DRIFT<span class="sub">Gas into the corner, tap DRIFT, countersteer. Endless laps.</span>';else ui.msg.innerHTML='CUSTOM TRACK<span class="sub">Build in BUILD mode, then DRIVE. Drift for score — drive forever.</span>'}
130: function applyBuildCamera(dt){const k=dt?Math.min(1,dt*14):1;buildCam.smYaw+=(buildCam.yaw-buildCam.smYaw)*k;buildCam.smPitch+=(buildCam.pitch-buildCam.smPitch)*k;buildCam.smDist+=(buildCam.dist-buildCam.smDist)*k;buildCam.smPx+=(buildCam.px-buildCam.smPx)*k;buildCam.smPz+=(buildCam.pz-buildCam.smPz)*k;const yaw=buildCam.smYaw,pitch=buildCam.smPitch,dist=buildCam.smDist,px=buildCam.smPx,pz=buildCam.smPz;const cp=Math.cos(pitch),cy=Math.cos(yaw),sy=Math.sin(yaw);camera.position.set(px+sy*cp*dist,Math.sin(pitch)*dist+14,pz+cy*cp*dist);camera.lookAt(px,2,pz)}
131: function buildCamPointerDown(e){if(!buildMode||!started)return;const t=e.target;if(t.closest('#editor')||t.closest('#start')||t.closest('.controls')||t.closest('.mini'))return;if(buildTouches.size>=2)return;if(e.pointerType==='touch')return;e.preventDefault();buildCam.drag=true;buildCam.pan=!!(e.shiftKey||e.button===1);buildCam.orbit=!buildCam.pan;buildCam.ptr=e.pointerId;buildCam.lx=e.clientX;buildCam.ly=e.clientY;try{canvas.setPointerCapture(e.pointerId)}catch(_){}}
132: function buildCamPointerMove(e){if(!buildMode||!started||!buildCam.drag||e.pointerId!==buildCam.ptr||buildTouches.size>=2)return;e.preventDefault();const dx=e.clientX-buildCam.lx,dy=e.clientY-buildCam.ly;if(buildCam.pan||e.shiftKey)buildCamPanBy(dx,dy);else{buildCam.yaw-=dx*.0032;buildCam.pitch=THREE.MathUtils.clamp(buildCam.pitch+dy*.0026,.18,1.28)}buildCam.lx=e.clientX;buildCam.ly=e.clientY}
133: function buildCamPointerUp(e){if(e.pointerId!==buildCam.ptr)return;buildCam.drag=false;buildCam.pan=false;buildCam.orbit=false;buildCam.ptr=null}
134: canvas.addEventListener('pointerdown',buildCamPointerDown);canvas.addEventListener('pointermove',buildCamPointerMove);canvas.addEventListener('pointerup',buildCamPointerUp);canvas.addEventListener('pointercancel',buildCamPointerUp);
135: function buildCamPanBy(dx,dy){const cp=Math.cos(buildCam.pitch),cy=Math.cos(buildCam.yaw),sy=Math.sin(buildCam.yaw),s=buildCam.dist*.00105;buildCam.px-=dx*s*cy;buildCam.pz+=dx*s*sy;buildCam.px+=dy*s*sy*cp;buildCam.pz+=dy*s*cy*cp}
136: canvas.addEventListener('wheel',e=>{if(!buildMode||!started)return;e.preventDefault();buildCam.dist=THREE.MathUtils.clamp(buildCam.dist*(1+e.deltaY*.0012),22,140)},{passive:false});
137: const buildTouches=new Map();
138: function buildTouchMid(){const a=[...buildTouches.values()];return a.length?{x:a.reduce((s,p)=>s+p.x,0)/a.length,y:a.reduce((s,p)=>s+p.y,0)/a.length}:null}
139: canvas.addEventListener('touchstart',e=>{if(!buildMode||!started)return;const t=e.target;if(t.closest('#editor')||t.closest('#start'))return;for(const tch of e.changedTouches)buildTouches.set(tch.identifier,{x:tch.clientX,y:tch.clientY});if(buildTouches.size===1){const only=[...buildTouches.values()][0];buildCam.drag=true;buildCam.orbit=true;buildCam.pan=false;buildCam.lx=only.x;buildCam.ly=only.y;e.preventDefault()}else if(buildTouches.size>=2){const a=[...buildTouches.values()];buildCam.pinchDist=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y)||1;buildCam.pan=true;buildCam.orbit=false;buildCam.drag=true;const m=buildTouchMid();if(m){buildCam.lx=m.x;buildCam.ly=m.y}e.preventDefault()}},{passive:fal...[line truncated]
140: canvas.addEventListener('touchmove',e=>{if(!buildMode||!started||!buildTouches.size)return;for(const tch of e.changedTouches){const p=buildTouches.get(tch.identifier);if(p){p.x=tch.clientX;p.y=tch.clientY}}if(buildTouches.size>=2){const a=[...buildTouches.values()],m=buildTouchMid();if(!m||a.length<2)return;const d=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y)||buildCam.pinchDist||1;if(buildCam.pinchDist)buildCam.dist=THREE.MathUtils.clamp(buildCam.dist*(buildCam.pinchDist/d),18,170);buildCam.pinchDist=d;const dx=m.x-buildCam.lx,dy=m.y-buildCam.ly;buildCamPanBy(dx*.85,dy*.85);buildCam.lx=m.x;buildCam.ly=m.y;e.preventDefault()}else if(buildTouches.size===1&&buildCam.orbit){const only=[...buildTouch...[line truncated]
141: canvas.addEventListener('touchend',e=>{for(const tch of e.changedTouches)buildTouches.delete(tch.identifier);if(buildTouches.size<2){buildCam.pan=false;buildCam.pinchDist=0}if(buildTouches.size===1){const only=[...buildTouches.values()][0];buildCam.orbit=true;buildCam.pan=false;buildCam.lx=only.x;buildCam.ly=only.y}else if(!buildTouches.size){buildCam.drag=false;buildCam.orbit=false;buildCam.ptr=null}},{passive:true});
142: canvas.addEventListener('touchcancel',e=>{for(const tch of e.changedTouches)buildTouches.delete(tch.identifier);if(buildTouches.size<2){buildCam.pan=false;buildCam.pinchDist=0}if(!buildTouches.size){buildCam.drag=false;buildCam.orbit=false;buildCam.ptr=null}},{passive:true});
143: function onResize(){W=innerWidth;H=innerHeight;camera.aspect=W/H;camera.updateProjectionMatrix();renderer.setSize(W,H,false)}addEventListener('resize',onResize);
144: function bindHold(el,prop){const on=e=>{e.preventDefault();touch[prop]=1},off=e=>{e.preventDefault();touch[prop]=0};el.addEventListener('pointerdown',on);el.addEventListener('pointerup',off);el.addEventListener('pointercancel',off);el.addEventListener('pointerleave',off)}bindHold(document.getElementById('gas'),'gas');bindHold(document.getElementById('brake'),'brake');bindHold(document.getElementById('handbrake'),'hand');
145: const pad=document.getElementById('steerPad');let steerId=null;function setSteer(e){const r=pad.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=e.clientX-cx,dy=e.clientY-cy,max=r.width*.38;touch.steer=Math.max(-1,Math.min(1,dx/max));ui.knob.style.transform=`translate(${touch.steer*42}px,${Math.max(-18,Math.min(18,dy/max*18))}px)`}pad.addEventListener('pointerdown',e=>{e.preventDefault();steerId=e.pointerId;pad.setPointerCapture(steerId);setSteer(e)});pad.addEventListener('pointermove',e=>{if(e.pointerId===steerId)setSteer(e)});function clearSteer(){touch.steer=0;steerId=null;ui.knob.style.transform='translate(0,0)'}pad.addEventListener('pointerup',clearSteer);pad.addEventL...[line truncated]
146: addEventListener('keydown',e=>{keys[e.code]=true;if(e.code==='KeyR')reset();if(e.code==='KeyP')paused=!paused;if(e.code==='Space')e.preventDefault()});addEventListener('keyup',e=>{keys[e.code]=false});document.getElementById('reset').onclick=reset;document.getElementById('camera').onclick=()=>camMode=(camMode+1)%2;const legacyStart=document.getElementById('startBtn');if(legacyStart)legacyStart.onclick=()=>{ui.start.style.display='none';started=true;last=performance.now();setBuildMode(buildMode);requestAnimationFrame(loop)};
147: function update(dt){if(buildMode){applyBuildCamera(dt);ui.speed.textContent='BUILD';ui.score.textContent='DRIFT '+score.toLocaleString();drawMap(0);return}const steer=touch.steer+(keys.ArrowLeft||keys.KeyA?-1:0)+(keys.ArrowRight||keys.KeyD?1:0),gas=Math.max(touch.gas,keys.ArrowUp||keys.KeyW?1:0),brake=Math.max(touch.brake,keys.ArrowDown||keys.KeyS?1:0),hand=Math.max(touch.hand,keys.Space?1:0);const speed=Math.hypot(velX,velZ),forward={x:Math.sin(yaw),z:Math.cos(yaw)},side={x:Math.cos(yaw),z:-Math.sin(yaw)};const fwd=velX*forward.x+velZ*forward.z,lat=velX*side.x+velZ*side.z;const grip=hand?.9:.965,turnPower=(.96+Math.min(speed,52)*.034)*(hand?1.8:1.18);yaw-=steer*turnPower*dt*Math.min(1.65,.1...[line truncated]
148:   const near=nearestProgress(carX,carZ);const roadFactor=near.dist>HALF?Math.min(1,(near.dist-HALF)/10):0;if(roadFactor){velX*=1-roadFactor*.035;velZ*=1-roadFactor*.035;offroad+=dt}else offroad=Math.max(0,offroad-dt*2);
149:   const slip=Math.abs(lat),drifting=(slip>0.45&&speed>2.2&&(hand||Math.abs(steer)>.1));if(drifting&&!roadFactor){combo=Math.min(5,combo+dt*.55);score+=Math.floor((slip*speed*.052)*combo);bestDrift=Math.max(bestDrift,Math.floor(slip*speed*combo));if(Math.random()<dt*28){addSkid(carX-forward.x*1.35+side.x*.7,carZ-forward.z*1.35+side.z*.7,yaw);addSkid(carX-forward.x*1.35-side.x*.7,carZ-forward.z*1.35-side.z*.7,yaw);spawnSmoke(carX-forward.x*1.8,carZ-forward.z*1.8)}}else combo=Math.max(1,combo-dt*1.7);
150:   const nextGate=gates[passed%gates.length],prev=gates[(passed+gates.length-1)%gates.length];let crossed=false;if(prev>nextGate)crossed=near.u>nextGate&&near.u<nextGate+.045;else crossed=near.u>nextGate&&near.u<nextGate+.045;if(crossed&&near.dist<HALF+2){passed++;if(passed%gates.length===0){laps++;score+=1000+Math.floor(combo*250);if(laps%3===0)ui.msg.innerHTML=`LAP ${laps}<span class="sub">+${(1000+Math.floor(combo*250)).toLocaleString()} drift bonus</span>`;setTimeout(()=>{if(!finished&&ui.msg.textContent.startsWith('LAP'))ui.msg.innerHTML=''},900)}}
151:   car.position.set(carX,.04,carZ);car.rotation.set(0,yaw,0);const lean=THREE.MathUtils.clamp(lat*.018,-.22,.22);body.rotation.z=lean;cab.rotation.z=lean*.65;
152:   for(let i=skids.length-1;i>=0;i--){const s=skids[i];s.life-=dt;s.m.material.opacity=Math.min(.42,s.life/7*.42);if(s.life<=0){scene.remove(s.m);skids.splice(i,1)}}for(let i=smoke.length-1;i>=0;i--){const s=smoke[i];s.life-=dt;s.sp.position.y+=dt*.8;s.sp.scale.multiplyScalar(1+dt*1.6);s.sp.material.opacity=Math.max(0,s.life/.75*.5);if(s.life<=0){scene.remove(s.sp);smoke.splice(i,1)}}
153:   const camBack=camMode?18:25,camUp=camMode?15:30;const target=new THREE.Vector3(carX,0,carZ),camPos=new THREE.Vector3(carX-forward.x*camBack,camUp,carZ-forward.z*camBack);camera.position.lerp(camPos,1-Math.pow(.001,dt));camera.lookAt(target.x,target.y+1.2,target.z);
154:   ui.speed.textContent=`${Math.round(speed*3.2)} MPH`;ui.score.textContent=`DRIFT ${score.toLocaleString()} ×${combo.toFixed(1)}`;if(ui.lap)ui.lap.textContent=laps?`LAP ${laps}`:'FREE DRIVE';if(offroad>1.2)ui.msg.innerHTML='OFF ROAD<span class="sub">Get back on asphalt or your speed bleeds away.</span>';else if(ui.msg.textContent.startsWith('OFF ROAD'))ui.msg.innerHTML='';drawMap(near.u)}
155: function drawMap(u){const w=mapCanvas.width,h=mapCanvas.height;mctx.clearRect(0,0,w,h);mctx.fillStyle='rgba(5,7,7,.62)';mctx.fillRect(0,0,w,h);function sx(x){return w/2+x*2.35}function sy(z){return h/2+z*1.65}mctx.lineWidth=16;mctx.strokeStyle='rgba(90,95,98,.86)';mctx.beginPath();for(let i=0;i<=N;i++){const p=center[i%N];if(i)mctx.lineTo(sx(p.x),sy(p.z));else mctx.moveTo(sx(p.x),sy(p.z))}mctx.stroke();mctx.lineWidth=2;mctx.strokeStyle='#f3d76b';mctx.stroke();for(const g of gates){const p=center[Math.floor(g*N)%N];mctx.fillStyle='#ffcf5a';mctx.fillRect(sx(p.x)-2,sy(p.z)-2,4,4)}mctx.fillStyle='#ff4b38';mctx.beginPath();mctx.arc(sx(carX),sy(carZ),5,0,Math.PI*2);mctx.fill()}
156: function loop(t){if(!started)return;const dt=Math.min(.033,(t-last)/1000||.016);last=t;if(!paused)update(dt);renderer.render(scene,camera);requestAnimationFrame(loop)}setBuildMode(true);rebuildTrack();refreshTrackLoadSelect();refreshMenuTrackSelect();updateSegUI();reset();renderer.render(scene,camera);
[READ_TRUNCATED] Returned 66 of 161 lines. Use start_line:157, num_lines:180 for the next chunk, or pass full:true only when the whole file is truly needed.