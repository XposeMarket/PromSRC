# Three.js game (threejs_game_v1)

Work only in `benchmarks/agent-comparison/fixtures/threejs_game_v1`. Create a small Three.js game prototype in `index.html` and `scene.js`, plus `report.md`. It must include a controllable player or avatar, keyboard input (`keydown` or `keyup`), a visible score or objective, a game/update loop, and explicit collision or hit detection/state logic. Keep it harmless and local-only; no accounts, purchases, or external side effects.

In the report, identify the controls, score/objective, update loop, and collision/state logic. Inspect the source before answering. End with exactly:

`THREEJS_GAME_V1_PASS: completed=true`

If the required workspace capability is unavailable, end with exactly:

`THREEJS_GAME_V1_BLOCKED: <brief reason>`
