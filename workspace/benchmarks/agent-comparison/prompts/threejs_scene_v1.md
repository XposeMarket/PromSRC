# Three.js scene (threejs_scene_v1)

Work only in `benchmarks/agent-comparison/fixtures/threejs_scene_v1`. Create a Three.js scene in `index.html` and `scene.js`, plus `report.md`. Include a `THREE.Scene`, camera, renderer, ambient or hemisphere light, directional or point light, and at least three distinct meshes added to the scene. Include a render loop and describe the scene graph in the report.

Use local-only files or clearly documented CDN loading. Do not log in or perform external side effects. Inspect the source before answering. End with exactly:

`THREEJS_SCENE_V1_PASS: completed=true`

If the required workspace capability is unavailable, end with exactly:

`THREEJS_SCENE_V1_BLOCKED: <brief reason>`
