# Three.js object (threejs_object_v1)

Work only in `benchmarks/agent-comparison/fixtures/threejs_object_v1`. Create a self-contained Three.js object demo using `index.html` and `scene.js`, plus `report.md`. The demo must define a camera and renderer, create at least one visible `THREE.Mesh` with geometry and material, and animate or render it through a loop. Prefer local or clearly documented CDN loading; do not use credentials or perform external side effects.

In `report.md`, list the geometry, material, camera, renderer, and verification performed. Inspect the source before answering. End with exactly:

`THREEJS_OBJECT_V1_PASS: completed=true`

If the required workspace capability is unavailable, end with exactly:

`THREEJS_OBJECT_V1_BLOCKED: <brief reason>`
