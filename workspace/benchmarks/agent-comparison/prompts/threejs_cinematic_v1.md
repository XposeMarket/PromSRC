# Three.js cinematic (threejs_cinematic_v1)

Work only in `benchmarks/agent-comparison/fixtures/threejs_cinematic_v1`. Create a short Three.js cinematic scene in `index.html` and `scene.js`, plus `report.md`. Include a composed environment with multiple objects/lights, a camera choreography or timeline with at least two timed beats/keyframes, and an animation/render loop. The report must describe the beats and how timing is represented in source.

Use local-only files or clearly documented CDN loading. Do not log in or perform external side effects. Inspect the source before answering. End with exactly:

`THREEJS_CINEMATIC_V1_PASS: completed=true`

If the required workspace capability is unavailable, end with exactly:

`THREEJS_CINEMATIC_V1_BLOCKED: <brief reason>`
