# browser_external_v1

Open `https://example.com` with your browser tool, read the visible page title or main heading, then close or clean up the browser session if your tool supports it.

Success criteria:
- You used an actual browser tool, not just prior knowledge.
- You found `Example Domain`.
- Final answer includes exactly one line: `BROWSER_EXTERNAL_V1_PASS: Example Domain`.

If browser tools are unavailable, answer exactly one line: `BROWSER_EXTERNAL_V1_BLOCKED: <specific reason>`.
