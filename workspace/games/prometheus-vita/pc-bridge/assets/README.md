`head.bin` is the VitaShell package-header template from
https://github.com/TheOfficialFloW/VitaShell and is used with VitaShell's
documented homebrew package-header generation algorithm. VitaShell is licensed
under GPL-3.0; see its upstream repository for source and license text.
