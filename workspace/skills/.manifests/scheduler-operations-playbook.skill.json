{
  "schemaVersion": "prometheus-skill-bundle-v1",
  "entrypoint": "SKILL.md",
  "triggers": "scheduled job, schedule job, cron job, scheduler, subagent schedule, remove subagent, update scheduled job, job schedule, automation schedule, scheduled jobs, schedule owner, heartbeat schedule, stuck scheduled job, run scheduled job, pause schedule, resume schedule, delete scheduled job, scheduler operations, schedule management",
  "id": "scheduler-operations-playbook"
}
