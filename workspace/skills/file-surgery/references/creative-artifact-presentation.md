# Creative vs one-shot artifact presentation (Raul preference)

**Auto `present_file`:** Creative Mode / editable artifacts only — HyperFrames, HTML Motion, Creative canvas/video exports, similar creative-workspace outputs.

**Do not auto `present_file`:** One-shot `generate_image` / `generate_video` (xAI/OpenAI) — those provider runs already auto-present to Raul. Report saved path unless he asks to open/present/send.

**Creative video pacing:** Hold readable text longer; slow transitions unless he asks for fast cuts.

**Landscape default** for Prometheus release-thread / promo HyperFrames videos unless he requests portrait.

**Visual taste:** Avoid purple-blue-cyan gradient “AI SaaS” looks unless brand-required; prefer editorial, industrial, real UI, physical materials.